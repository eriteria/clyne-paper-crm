# Zoho OAuth (development)
ZOHO_CLIENT_ID=1000.S8BF2EWQ4C6M90N7IM3YC9EFROCKPL
ZOHO_CLIENT_SECRET=3a8732c38ee139ad9b1f55ed30c0b9d683c3a27cb0
ZOHO_REDIRECT_URI=http://localhost:5000/api/auth/zoho/callback
ZOHO_DC=us
# For sign-in only, Accounts scopes are enough
ZOHO_SCOPES=AaaServer.profile.READ
# Use online for no refresh token, or offline if you want refresh token
ZOHO_ACCESS_TYPE=online


# JWT settings (already used by the app)
# JWT_SECRET=your_jwt_secret
# JWT_REFRESH_SECRET=your_jwt_refresh_secret
# JWT_EXPIRES_IN=1h
# JWT_REFRESH_EXPIRES_IN=7d

# Frontend URL used for redirect after OAuth completes
FRONTEND_URL=http://localhost:3000

# Database
DATABASE_URL="postgresql://username:password@localhost:5432/clyne_paper_crm"
# For MySQL/cPanel: DATABASE_URL="mysql://username:password@localhost:3306/clyne_paper_crm"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-this-in-production"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# Server
PORT=5000
NODE_ENV="development"

# Email (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
FROM_EMAIL="noreply@clynepaper.com"

# File uploads
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Cors
FRONTEND_URL="http://localhost:3000"
